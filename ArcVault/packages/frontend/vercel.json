{
  "framework": "nextjs",
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "prisma generate --schema=./prisma/schema.prisma && next build",
  "crons": [
    {
      "path": "/api/cron/process-delays",
      "schedule": "0 0 * * *"
    }
  ]
}
